# Handoff prompt for the build session

Paste this into a fresh Claude Code session in this repo to start the v0
build.

---

I'm building the v0 of an AI assistant for the MLSO Podium committee. The
design is settled and documented. Read `CLAUDE.md` first, then
`docs/build-plan.md`. Resume from the first unchecked phase. Update the
checkbox and commit when each phase is done. Don't change the design
without flagging it — if something seems off, ask before reversing it
(read `docs/decisions.md` for rationale on settled choices).

Start with phase 1.
